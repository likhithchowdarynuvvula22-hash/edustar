# Railway deployment config
# Tells Railway which command to run to start the server

[build]
builder = "nixpacks"

[deploy]
startCommand = "node --experimental-sqlite edustar-backend.mjs"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
